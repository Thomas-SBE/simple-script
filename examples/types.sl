function essai(a: int, b: int): int => {
    if(a == b) => return a;
    else => return b;
}

function valid(a: bool, b: int): int => {
    if(false) => return b;
    else => b = b * 2;
    return b;
}

